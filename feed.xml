---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }}</title>
  <link href="{{ site.url }}{{ site.baseurl }}/feed.xml" rel="self"/>
  <link href="{{ site.url }}{{ site.baseurl }}/"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}{{ site.baseurl }}/</id>
  {% assign all_content = site.monographies | concat: site.biosemiotique %}
  {% assign all_content = all_content | concat: site.terrain %}
  {% assign all_content = all_content | concat: site.notes | sort: 'date' | reverse %}
  {% for item in all_content limit: 30 %}
  <entry>
    <title>{{ item.title | xml_escape }}</title>
    <link href="{{ item.url | absolute_url }}"/>
    <updated>{{ item.date | date_to_xmlschema }}</updated>
    <id>{{ item.url | absolute_url }}</id>
    <summary>{{ item.description | default: item.excerpt | strip_html | xml_escape }}</summary>
  </entry>
  {% endfor %}
</feed>
